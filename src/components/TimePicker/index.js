import React from 'React';
import moment from 'moment';

export default class TimePickerModule extends React.Component {
  state = {
    date: moment(),
  }
  render () {
    return (
      <div>wq</div>
    )
  }
}
